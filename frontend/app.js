const visitorCount = document.getElementById("visitor-count");
const visitorCountBanner = document.getElementById("visitor-count-banner");

const placeholderValue = "Phase 1 Pending";

if (visitorCount) {
  visitorCount.textContent = placeholderValue;
}

if (visitorCountBanner) {
  visitorCountBanner.textContent = placeholderValue;
}